# Server Configuration
# Copy this file to .env and adjust values as needed.
# All variables have safe defaults — the app runs without a .env file.

# Host address the server binds to (string, default: 127.0.0.1)
HOST=127.0.0.1

# Port the server listens on (integer, default: 3000)
PORT=3000
